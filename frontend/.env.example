# Frontend env example for development
# Optional: override backend used by Vite dev server proxy
# Example: BACKEND_URL=http://localhost:3006
BACKEND_URL=http://localhost:3006

# If you prefer Vite-style env (accessible as import.meta.env.VITE_API_URL),
# add a VITE_ prefixed variable. Not required because frontend uses relative /api paths.
#VITE_API_URL=http://localhost:3006
